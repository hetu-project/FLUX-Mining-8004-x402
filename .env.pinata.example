# Pinata IPFS Configuration Template
# Copy this file to .env.pinata and add your own credentials

# Enable IPFS storage for VLC graph data (set to "true" to enable, "false" or omit to use traditional on-chain storage)
USE_PINATA="true"

# Public/Private Access Control
# "true" = files accessible via any IPFS gateway (recommended for transparency)
# "false" = files only accessible via authenticated Pinata gateway
PINATA_PUBLIC="false"

# Pinata API Credentials
# Get your credentials from https://app.pinata.cloud/developers/api-keys
API_KEY_PINATA="your_api_key_here"
API_SECRET_PINATA="your_api_secret_here"
JWT_SECRET_ACCESS="your_jwt_token_here"

# Pinata Gateway (for retrieving IPFS content via HTTP)
# Find this in your Pinata dashboard under Gateways
GATEWAY_PINATA="your-gateway.mypinata.cloud"
